<?xml version="1.0" encoding="utf-8"?>
<!--
    Shown in the widget picker on API 31+. Static by design: the picker renders this
    without running the app, so it advertises the shape rather than live data.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp"
    android:background="@color/background_dark"
    tools:ignore="Overdraw">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_heading"
        android:textColor="@color/primary_blue"
        android:textSize="11sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/widget_preview_word"
        android:textColor="@color/on_background_light"
        android:textSize="22sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_preview_translation"
        android:textColor="@color/on_surface_variant"
        android:textSize="14sp" />

</LinearLayout>
