<?xml version="1.0" encoding="utf-8"?>
<!--
    description, previewLayout and the targetCell sizes are all API 31+ and all
    expected by the modern picker. Without them the entry showed no explanation, no
    preview and no default size.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewLayout="@layout/word_widget_preview"
    android:minWidth="100dp"
    android:minHeight="100dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="86400000"
    android:widgetCategory="home_screen" />
