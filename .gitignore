*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
# Per-developer IDE state: which device was last deployed to, and per-session
# assistant bookkeeping. Neither describes the project.
/.idea/deploymentTargetSelector.xml
/.idea/planningMode.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/

# Release signing material. keystore.properties holds the store and key passwords
# in plaintext; neither it nor the keystore itself belongs in the repository.
keystore.properties
*.jks
*.keystore

# Session tooling metadata, not part of the project.
.reasonix/

# Vercel CLI link state (project/org ids, machine-local). web/.gitignore
# already ignores this; the root needs it too now that deploys run from here.
.vercel/
